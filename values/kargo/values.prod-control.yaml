# Prod-control cluster overrides (combines prod environment + control cluster)
api:
  host: "kargo-prod.localhost"

projectConfig:
  autoPromotionEnabled: false

warehouse:
  enabled: true
  allowTags: "^[a-z0-9-]+-[0-9]+\\.[0-9]+\\.[0-9]+$"
